import { Link} from "react-router-dom";

const Button = (props)=>{

    let inpunts= ["FIRST NAME"," LAST NAME","STREET ADDRESS, CITY, STATE","APT #","EMAIL","PHONE NUMBER"]
   
    return(<div>


<input type="text" placeholder ="hola"></input>
    </div>)
}

export default Button;